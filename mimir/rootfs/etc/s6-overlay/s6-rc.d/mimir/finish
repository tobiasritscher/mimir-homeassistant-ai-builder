#!/command/with-contenv bash
# shellcheck shell=bash
# ==============================================================================
# Mímir Service Finish Script
# ==============================================================================

echo "Mímir service stopped with exit code: $1"

# If the service crashed, wait before restart
if [ "$1" -ne 0 ]; then
    echo "Service crashed, waiting 5 seconds before restart..."
    sleep 5
fi
